kind = "web"
previewPath = "/"
title = "BrowserScan - Browser Fingerprint Tool"
version = "1.0.0"
id = "artifacts/browserscan"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/browserscan run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/browserscan", "run", "build" ]
publicDir = "artifacts/browserscan/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
